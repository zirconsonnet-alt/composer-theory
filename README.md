# composer-theory packaging workspace

This repository root contains the packaging configuration used to build and
publish the `composer-theory` distribution from the `composer_theory/` source
tree.

The full package documentation used for the PyPI long description lives in
`composer_theory/README.md`.
